def add_three(number)
  number + 3
end

returned_value = add_three(4)
puts returned_value

#Ruby always returns the evaluated result of the last line of an expression **unless** an explicit return comes before it then it only returns the explicit value